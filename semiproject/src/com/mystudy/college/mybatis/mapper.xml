<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="college">

	
	<insert id="insert" parameterType="com.mystudy.college.model.vo.CustomerVO">
		INSERT INTO
		CUSTOMER
		VALUES(#{C_ID }
				#{C_PASSWORD }
				#{C_NAME }
				#{C_RRN }
				#{C_ACOUNT }
				#{C_AUTHORITY }
				#{C_GENDER }
				#{C_CALLNUMBER }
				#{C_PHONE }
				#{C_EMAIL }
				#{M_ADDRESS })
	</insert>

>>>>>>> branch 'master' of https://github.com/kec0486/itwillsemiproject.git
	<!-- 
	전체 직원 목록 조회
	<select id="list" resultType="EmployeeVO">
		SELECT * FROM EMPLOYEES ORDER BY EMPLOYEE_ID
	</select>
	
	이름으로 검색(조회)
	<select id="fullnameList" parameterType="string" resultType="EmployeeVO">
		SELECT *
		FROM EMPLOYEES
		WHERE FIRST_NAME = INITCAP(#{fullname})
		OR LAST_NAME = INITCAP(#{fullname})
		ORDER BY EMPLOYEE_ID
	</select>
	
	동적검색
	<select id="search" parameterType="map" resultType="EmployeeVO">
		SELECT *
		FROM EMPLOYEES
		<where>
			<if test="idx == 0">
				EMPLOYEE_ID = #{keyword}
			</if>
			<if test="idx == 1">
				FIRST_NAME = INITCAP(#{keyword}) OR LAST_NAME = INITCAP(#{keyword})
			</if>
			<if test="idx == 2">
				JOB_ID = INITCAP(#{keyword})
			</if>
			<if test="idx == 3">
				DEPARTMENT_ID = #{keyword}
			</if>
		</where>
	</select> -->
	
	
	
	<!-- 수강(수업) 등록  -->
	<insert id="lectureInsert" parameterType="com.mystudy.college.model.vo.LectureVO">
	INSERT INTO LECTURE 
	(L_ID, L_DEPARTMENT, L_CATEGORIZE, L_YEAR, L_SEMESTER, L_NAME, L_PROFESSOR, L_CREDIT )
	VALUES (#{l_id }, #{l_department }, #{l_categorize }, #{l_year }, #{l_semester }, #{l_name }, #{l_professor }, #{l_credit })
	</insert>
	
	<insert id="timeInsert" parameterType="com.mystudy.college.model.vo.TimeVO">
	INSERT INTO TIME
	(T_ID, T_DAY, T_TIME )
	VALUES (#{t_id }, #{t_day },#{t_time })
	</insert>
</mapper>







